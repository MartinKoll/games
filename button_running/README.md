# Button running

Small game where the button is escaping the cursor, where the speed is determined by distance from cursor. Made completely without AI as an introduction to pygames

## To run

Install pygames using

```
python3 -m pip install -U pygame --user
```